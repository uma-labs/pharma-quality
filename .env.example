# Databricks Connection — copy this file to .env and fill in your values
# NEVER commit .env to git (it is already in .gitignore)

# Workspace URL (no trailing slash)
# AWS example:   https://dbc-xxxxxxxx-yyyy.cloud.databricks.com
# Azure example: https://adb-1234567890123456.12.azuredatabricks.net
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com

# Personal Access Token
# Generate in: Databricks UI → top-right avatar → Settings → Access tokens → Generate new token
DATABRICKS_TOKEN=dapi...

# SQL Warehouse HTTP Path
# Find in: Databricks UI → SQL Warehouses → click your warehouse → Connection details → HTTP path
# Example: /sql/1.0/warehouses/abcdef1234567890
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your_warehouse_id

# Unity Catalog name (will be created if it does not exist)
DATABRICKS_CATALOG=pharma_quality
